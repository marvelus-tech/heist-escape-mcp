{
  "name": "@heist-escape/mcp-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "seed": "wrangler d1 execute heist-db --local --file=./schema.sql && wrangler d1 execute heist-db --local --file=./seed.sql",
    "seed:remote": "wrangler d1 execute heist-db --file=./schema.sql && wrangler d1 execute heist-db --file=./seed.sql"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "agents": "^0.1.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231218.0",
    "wrangler": "^3.22.1"
  }
}
